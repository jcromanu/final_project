apiVersion: v1
kind: Service
metadata:
  name: {{ include "projectchart.fullname" . }}
  labels:
    {{- include "projectchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "projectchart.selectorLabels" . | nindent 4 }}
